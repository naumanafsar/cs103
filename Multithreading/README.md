# Windows
If you are on windows, go ahead and compile the code,

# Linux
If you are on Linux and using g++ compiler then you might want to compiler and library support for the ISO C++ 2011 standard.
You can enable & compile it by executing  `g++ filename.cpp -o filename -std=gnu++11 -lpthread`
